# Configuração PostgreSQL (Supabase ou Render)
DB_ENGINE=postgres
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=sua_senha
DB_HOST=seu_host
DB_PORT=5432

# Para Render (opcional - será fornecido automaticamente)
DATABASE_URL=postgresql://user:password@host:port/database

# Configurações da aplicação
PORT=8000
DEBUG=False
SECRET_KEY=sua_secret_key_aqui

# Para Render (será definido automaticamente)
RENDER=true
RENDER_EXTERNAL_HOSTNAME=seu-app.onrender.com